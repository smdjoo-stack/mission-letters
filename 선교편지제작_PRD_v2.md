# 선교편지제작 — 제품 요구사항 정의서 (PRD v2)

> **개정 이력**
> - v1 (2026-09-01): 최초 작성
> - **v2 (2026-09-01): 아래 3가지 구조적 문제를 해결하고, v1에서 비어 있던 데이터 모델·URL 구조·저장 방식을 확정.**
>   1. 로컬 저장과 GitHub Pages 공유가 단절되어 있던 문제 → **앱 내 발행(GitHub API 직접 커밋)** 으로 통합
>   2. 편지 원문이 공개 저장소에 평문 노출되던 문제 → **본문 암호화 저장**으로 실제 잠금 구현
>   3. 구글 드라이브 공유 링크가 이미지로 표시되지 않던 문제 → **링크 정규화 + 즉시 검증**
>   4. 실사용 불가였던 범위 → **편지 수정을 MVP로 승격**
> - **v2.1 (2026-09-01): 후원자용 지난 편지 보관함 추가** (§7.5). 편지가 매달 쌓이는 서비스인데
>   v2 까지는 후원자가 링크를 받은 그 한 통만 볼 수 있었다. 최신 편지가 먼저 보이고,
>   지난 편지도 찾아 읽을 수 있게 한다.

> **코딩 에이전트 실행 요청문**
> 아래 명세를 바탕으로 정적 웹앱을 구현해 주세요. 서버·데이터베이스 없이 동작하며, GitHub 저장소를 데이터 저장소로 사용하고 GitHub Pages로 배포·공유합니다. 편지 본문은 브라우저에서 암호화한 뒤 저장하므로 저장소가 공개되어도 내용이 노출되지 않습니다. 사진은 파일 업로드가 아니라 구글 드라이브 공유 링크를 붙여넣어 표시합니다. 화면은 반응형(모바일 우선)이며 A4 PDF 출력을 지원합니다. **P0(MVP)** 로 표시된 작업을 먼저 완성한 뒤 P1로 진행해 주세요.

---

## 1. 프로젝트 개요

- **서비스명:** 선교편지제작
- **한 줄 정의:** 선교사가 후원자와 후원교회에 보낼 선교편지를 작성하고 공유하는 웹앱.
- **대상 사용자**
  - **작성자(선교사)** — 매월 말 사역 소식을 정리해 편지를 작성하고 발행한다. 기술 배경을 가정하지 않는다.
  - **열람자(후원자·후원교회)** — 공유받은 링크와 비밀번호로 편지를 읽는다. 대부분 모바일.
- **핵심 가치:** 별도 서버·비용 없이 편지를 예쁘게 만들어 공유하고, A4로 인쇄/PDF 저장까지 할 수 있다.
- **성공 기준:** 선교사가 **git 명령을 한 번도 쓰지 않고** 편지 작성부터 링크 공유까지 완료할 수 있다.

---

## 2. 기술 스택 및 구조

### 2.1 전체 구조

```
GitHub 저장소 (예: missionary/letters)
├── index.html          ← 앱 전체 (작성 화면 + 보기 화면)
├── assets/             ← CSS, JS
└── letters/
    ├── index.json      ← 편지 목록(메타데이터만)
    └── 2026-09.json    ← 편지 1건 (본문은 암호문)
         ↑
   GitHub Pages 로 배포 → https://missionary.github.io/letters/
```

- **프레임워크 없는 정적 웹앱** (HTML/CSS/바닐라 JS). 빌드 단계 없음 — 파일을 그대로 Pages가 서빙한다.
- **저장:** 편지 데이터는 같은 저장소의 `letters/` 폴더에 JSON으로 저장한다. 앱이 GitHub REST API로 직접 커밋한다.
- **배포·공유:** GitHub Pages. 커밋되면 수 초~1분 내 자동 반영.
- **사진:** 구글 드라이브 공유 링크를 붙여넣으면 이미지 직링크로 변환해 표시.
- **암호화:** 브라우저 내장 WebCrypto API. 외부 라이브러리 없음.
- **지원 환경:** 최신 Chrome / Safari / Edge / 삼성인터넷. HTTPS 필수(WebCrypto 요구사항 — Pages는 기본 HTTPS).

### 2.2 두 개의 모드

같은 `index.html`이 두 역할을 한다.

| 모드 | 진입 | 대상 | 하는 일 |
|---|---|---|---|
| **작성자 모드** | `#/list`, `#/write` 등 | 선교사 본인 | 작성·수정·발행·설정 |
| **열람 모드** | `#/letter/{id}` | 후원자 | 비밀번호 입력 후 읽기·인쇄 |

작성자 모드는 로그인이 아니라 **로컬에 저장된 GitHub 토큰의 존재 여부**로 구분한다. 토큰이 없는 방문자에게는 작성 화면이 노출되지 않는다(코드 자체는 공개이므로 이는 **접근 편의 구분이지 보안 경계가 아니다** — 실제 쓰기 권한은 토큰이 통제한다).

---

## 3. 데이터 모델

### 3.1 `letters/index.json` — 편지 목록

```json
{
  "schemaVersion": 1,
  "letters": [
    { "id": "2026-09", "publishedAt": "2026-09-01", "updatedAt": "2026-09-01T12:30:00Z" },
    { "id": "2026-08", "publishedAt": "2026-08-02", "updatedAt": "2026-08-02T09:10:00Z" }
  ]
}
```

- **제목을 넣지 않는다.** 이 파일은 공개 저장소에서 누구나 읽을 수 있으므로, 날짜 외의 정보를 두지 않는다.
- 목록은 `publishedAt` 내림차순으로 저장한다(최신순).

### 3.2 `letters/{id}.json` — 편지 1건

```json
{
  "schemaVersion": 1,
  "id": "2026-09",
  "publishedAt": "2026-09-01",
  "updatedAt": "2026-09-01T12:30:00Z",
  "crypto": {
    "alg": "AES-GCM",
    "kdf": "PBKDF2-SHA256",
    "iterations": 210000,
    "salt": "<base64, 16바이트>",
    "iv": "<base64, 12바이트>"
  },
  "hint": "후원자에게 안내한 비밀번호 힌트 (선택, 평문)",
  "ciphertext": "<base64>"
}
```

`ciphertext`를 복호화하면 아래 JSON이 나온다.

### 3.3 편지 본문 (복호화된 내용)

```json
{
  "title": "2026년 9월 선교편지",
  "authorName": "홍길동 선교사",
  "period": "2026년 9월",
  "greeting": "사랑하는 후원자님께",
  "blocks": [
    { "type": "text",  "value": "지난 한 달 동안..." },
    { "type": "image", "driveId": "1A2b3C...", "caption": "주일학교 아이들" },
    { "type": "text",  "value": "기도 제목은..." }
  ],
  "closing": "감사합니다."
}
```

- **블록 배열 구조**를 쓰는 이유: 문단 사이 원하는 위치에 사진을 끼워 넣기 위해서다(4.3의 핵심 요구사항).
- 사진은 URL 전체가 아니라 **드라이브 파일 ID만** 저장한다. 표시 시점에 URL로 조립한다.

### 3.4 로컬 설정 (`localStorage`, 저장소에 올라가지 않음)

| 키 | 내용 |
|---|---|
| `missionaryName` | 선교사 이름 |
| `repo` | `{ owner, name, branch }` |
| `githubToken` | GitHub 개인 액세스 토큰 |
| `defaultPassword` | 새 편지 작성 시 기본으로 채울 비밀번호 |
| `readerPassword:{id}` | (열람자) 한 번 입력한 비밀번호 기억 — 선택 |

---

## 4. URL 구조

해시 라우팅을 사용한다(GitHub Pages에서 새로고침 시 404가 나지 않도록).

| URL | 화면 |
|---|---|
| `/#/list` | 편지 목록 (작성자) |
| `/#/write` | 새 편지 작성 |
| `/#/write/2026-09` | 기존 편지 수정 |
| `/#/settings` | 설정 |
| `/#/letter/2026-09` | **편지 보기 — 후원자에게 공유하는 링크** |
| `/#/archive` | **지난 편지 보관함 (후원자용)** |
| `/` (해시 없음) | 후원자면 최신 편지로 이동, 작성자면 편지 목록으로 |

작성자가 후원자에게 보내는 것은 `https://<계정>.github.io/<저장소>/#/letter/2026-09` 형태의 링크 하나다.

---

## 5. 개발 작업 목록

### P0 — MVP (이것까지 되어야 실제로 쓸 수 있다)

| # | 작업 | 화면 | 완료 기준 |
|---|---|---|---|
| 1 | 초기 설정 마법사 | 설정 | 선교사 이름·저장소·GitHub 토큰을 입력해 저장하면, 저장소 접근 테스트가 통과하고 "연결됨"이 표시된다 |
| 2 | 편지 작성 | 작성 | 제목·인사말·본문 문단을 입력하고 임시저장하면 브라우저에 남아, 새로고침해도 내용이 유지된다 |
| 3 | 드라이브 사진 삽입 | 작성 | 공유 링크를 붙여넣으면 즉시 미리보기가 뜨고, 원하는 문단 사이에 배치된다. 실패 시 원인이 안내된다 (§6.2) |
| 4 | 본문 암호화 | 작성 | 발행 시 본문이 비밀번호로 암호화되어 저장되며, 저장소의 JSON 파일을 직접 열어도 내용을 읽을 수 없다 |
| 5 | **발행(원클릭 배포)** | 작성 | "발행" 버튼 한 번으로 `letters/{id}.json`과 `index.json`이 커밋되고, 공유 링크가 화면에 표시된다. **git 명령 불필요** |
| 6 | **편지 수정** | 목록·작성 | 발행한 편지를 다시 열어 고치고 재발행할 수 있다 |
| 7 | 편지 목록 | 목록 | 발행한 편지가 최신순으로 표시되고, 각 항목에서 수정·보기·링크 복사가 가능하다 |
| 8 | 편지 보기 + 비밀번호 | 보기 | 링크 접속 시 비밀번호를 묻고, 맞으면 편지가 표시된다. 틀리면 오류를 알린다 |
| 9 | A4 PDF 출력 | 보기 | "PDF로 저장" 시 사진이 모두 로드된 상태로 A4 레이아웃 인쇄 화면이 열린다 (§6.3) |
| 10 | 반응형 레이아웃 | 전체 | 모바일(360px)~데스크톱에서 가로 스크롤 없이 읽기 편하다 |
| 11 | **지난 편지 보관함** | 보관함 | 후원자가 최신 편지를 먼저 보고, 지난 편지도 찾아 읽을 수 있다 (§7.5) |

### P1 — 곧이어

| # | 작업 | 완료 기준 |
|---|---|---|
| 11 | 편지 삭제 | 확인 후 저장소에서 파일이 삭제되고 목록에서 사라진다 |
| 12 | 편지별 비밀번호 | 편지마다 다른 비밀번호를 지정할 수 있다 |
| 13 | 수동 백업/복원 | 편지 JSON을 내려받고, 파일에서 불러올 수 있다 |
| 14 | 토큰 없는 발행 경로 | 토큰 미설정 시 JSON 파일을 내려받고 업로드 방법을 안내한다 |
| 15 | 비밀번호 기억 | 후원자가 한 번 입력하면 다음 방문 시 자동 해제(체크박스) |

### P2 — 향후 검토

- 발송 대상(후원자·후원교회) 명단 관리
- 후원자 이메일 자동 발송 — **현 구조에서는 불가.** 별도 서버와 발송 서비스(Resend, SendGrid 등)가 필요하며 도입 시 비용·개인정보 보관 문제를 함께 재검토해야 한다.
- 편지 템플릿·테마 선택
- 조회 여부 확인

---

## 6. 핵심 기능 상세

### 6.1 발행 (P0-5) — v2의 가장 큰 변경

v1은 "로컬 파일로 저장"만 규정하여, 후원자가 링크로 편지를 보려면 선교사가 그 파일을 저장소에 직접 올려야 했다. v2는 앱이 이를 대신한다.

**발행 절차 (앱 내부 동작)**

1. 본문 JSON을 만들고 비밀번호로 암호화한다.
2. GitHub Contents API로 `letters/{id}.json`을 커밋한다.
   `PUT /repos/{owner}/{repo}/contents/letters/{id}.json`
   - 기존 파일 수정 시에는 GET으로 `sha`를 먼저 조회해 함께 보낸다(없으면 신규 생성).
3. `letters/index.json`을 읽어 항목을 추가/갱신하고 다시 커밋한다.
4. 공유 링크를 화면에 표시하고 복사 버튼을 제공한다.
5. Pages 반영에 최대 1분이 걸릴 수 있음을 안내한다.

**토큰**

- fine-grained personal access token, **해당 저장소 하나에만** `Contents: Read and write` 권한.
- `localStorage`에만 보관하며 저장소에 절대 커밋하지 않는다.
- 설정 화면에 경고를 명시한다: *"공용 컴퓨터에서는 사용하지 마세요. 토큰이 유출되면 GitHub에서 즉시 폐기할 수 있습니다."*
- 설정 마법사는 토큰 발급 화면 링크와 필요한 권한을 그림과 함께 안내한다.

**실패 처리**

| 상황 | 처리 |
|---|---|
| 토큰 만료·권한 부족 (401/403) | "토큰을 다시 발급해 주세요" + 설정 화면 이동 |
| 충돌 (409) | 최신 `index.json`을 다시 읽어 재시도 |
| 네트워크 오류 | 작성 내용은 브라우저에 보존하고 "다시 시도" 제공 — **입력을 절대 잃지 않는다** |

### 6.2 구글 드라이브 사진 (P0-3)

사용자가 드라이브에서 복사하는 링크는 그대로는 이미지로 표시되지 않는다. 아래 처리가 필수다.

**1단계 — 파일 ID 추출.** 다음 형태를 모두 인식한다.

```
https://drive.google.com/file/d/{ID}/view?usp=sharing
https://drive.google.com/open?id={ID}
https://drive.google.com/uc?id={ID}
https://docs.google.com/.../d/{ID}/...
{ID}  (ID만 붙여넣은 경우)
```

**2단계 — 표시 URL 조립.**

```
1순위: https://drive.google.com/thumbnail?id={ID}&sz=w1600
2순위: https://lh3.googleusercontent.com/d/{ID}=w1600
```

1순위가 실패하면 2순위로 자동 재시도한다.

**3단계 — 즉시 검증 (가장 중요).** 붙여넣는 순간 실제로 로드해 보고 결과를 보여준다.

> ⚠️ **작성자는 구글에 로그인되어 있어 문제를 알아채지 못한다.** 공유 설정이 "제한됨"이어도 작성자 화면에서는 사진이 보이지만 후원자에게는 보이지 않는다. 따라서 검증 실패 시뿐 아니라 발행 직전에도 **"사진 공유 설정이 '링크가 있는 모든 사용자'인지 확인하셨나요?"** 를 확인 항목으로 띄운다.

실패 시 안내 문구:

> 사진을 불러오지 못했습니다. 구글 드라이브에서 해당 사진의 **공유 → '링크가 있는 모든 사용자'** 로 설정한 뒤 링크를 다시 붙여넣어 주세요.

**제약 (문서화 필요)**

- 사진 자체는 구글 드라이브에서 공개로 제공되므로 **암호화 대상이 아니다.** 링크를 아는 사람은 사진을 볼 수 있다. 민감한 사진은 올리지 않도록 작성 화면에 안내한다.
- 구글이 대량 조회를 제한할 수 있다. 한 편지에 사진 15장 이하를 권장한다.

### 6.3 A4 PDF 출력 (P0-9)

- `@media print` 규칙으로 A4(210×297mm) 레이아웃을 정의한다. 별도 PDF 라이브러리를 쓰지 않는다.
- 여백 20mm, 본문 11pt, 사진은 페이지 폭에 맞추고 `break-inside: avoid`로 잘리지 않게 한다.
- 비밀번호 입력 화면·버튼 등은 `display: none`.
- **인쇄 전 모든 사진의 로딩 완료를 기다린 뒤** `window.print()`를 호출한다. 그러지 않으면 사진 없는 PDF가 만들어진다.
- 첫 페이지 상단에 선교사 이름과 편지 기간을 넣는다.

### 6.4 비밀번호 잠금 (P0-4, P0-8)

v1은 비밀번호를 코드에 두는 방식이라 잠금이 사실상 무의미했다(저장소의 JSON을 직접 열면 원문 노출). v2는 **본문 자체를 암호화**한다.

**암호화 (발행 시)**

```
salt   = 랜덤 16바이트
key    = PBKDF2-SHA256(비밀번호, salt, 210000회) → 256비트
iv     = 랜덤 12바이트
ciphertext = AES-GCM(key, iv, JSON.stringify(본문))
```

**복호화 (열람 시)** — 같은 파라미터로 키를 파생해 복호화한다. 실패하면 비밀번호 오류로 처리한다.

**이 방식으로 달성되는 것**

- 저장소가 공개여도 편지 원문·기도 제목·현지인 이름이 노출되지 않는다.
- 검색엔진과 GitHub 코드 검색에도 잡히지 않는다.
- 서버가 전혀 필요 없다.

**여전히 남는 한계 (반드시 문서화)**

- 비밀번호를 아는 사람은 누구에게든 내용을 전달할 수 있다.
- 비밀번호가 약하면(생일, `1234` 등) 오프라인 대입 공격이 가능하다. → **8자 이상을 요구하고, 4자리 숫자 등 약한 비밀번호는 경고한다.**
- 사진은 암호화되지 않는다(§6.2).
- 발행 날짜와 편지 개수는 공개된다.

> 저장소를 **private으로 두면서 Pages를 공개**하는 것도 가능하다(GitHub Pro 이상). 가능하다면 이 조합을 권장하며, 설정 안내에 포함한다. 단 무료 계정에서도 위 암호화만으로 실용적 수준이 확보된다.

---

## 7. 화면별 명세

### 7.1 편지 목록 (`#/list`)

- 발행한 편지가 **최신순**으로 표시된다. 각 항목: 기간 · 제목 · 발행일 · 마지막 수정일.
- 제목 표시를 위해 목록 화면에서도 복호화가 필요하다 → 설정의 기본 비밀번호로 자동 복호화하고, 실패한 항목은 "🔒 (다른 비밀번호)"로 표시한다.
- 항목별 동작: **수정 / 보기 / 링크 복사** (P1: 삭제)
- 상단에 "새 편지 쓰기" 버튼.
- **빈 상태:** "아직 작성한 편지가 없습니다. 첫 편지를 써 보세요."
- **미설정 상태:** 저장소·토큰이 없으면 설정 마법사로 유도한다.

### 7.2 편지 작성 (`#/write`, `#/write/{id}`)

- 입력: 기간(예: `2026년 9월`, ID 자동 생성 `2026-09`) · 제목 · 인사말 · 본문 블록 · 맺음말 · 비밀번호.
- **본문 편집:** 문단 블록과 사진 블록을 순서대로 쌓는다. 각 블록에 위/아래 이동, 삭제 버튼.
- **사진 추가:** "사진 추가" → 링크 붙여넣기 입력창 → 즉시 미리보기 → 설명(캡션) 입력. 붙여넣기 이벤트에서 드라이브 링크를 자동 감지해 바로 블록으로 만든다.
- **자동 임시저장:** 입력할 때마다 `localStorage`에 저장. 새로고침·브라우저 종료 후에도 복구된다.
- 하단 고정 바: **[미리보기] [발행]**
- 발행 전 확인 대화상자:
  - 비밀번호 확인 (8자 이상 검사)
  - 사진 공유 설정 확인 체크
  - 수정 발행인 경우 "기존 편지를 덮어씁니다" 안내
- 발행 후: 공유 링크 + 복사 버튼 + "후원자에게 링크와 비밀번호를 함께 알려주세요" 안내.

### 7.3 편지 보기 (`#/letter/{id}`) — 후원자용

**상태 1 — 잠김**
- 선교사 이름과 편지 기간만 표시(암호화되지 않은 메타데이터). 제목·본문은 보이지 않는다.
- 비밀번호 입력창 + 힌트(있으면) + "이 기기에서 기억하기" 체크박스(P1).

**상태 2 — 열림**
- 읽기 최적화: 본문 17px 이상, 행간 1.8, 최대 폭 680px, 넉넉한 문단 간격.
- 사진은 폭 100%, 캡션은 아래 작은 글씨. 탭하면 확대.
- 하단: **[PDF로 저장]** 버튼.

**상태 3 — 오류**
- 비밀번호 불일치: "비밀번호가 맞지 않습니다."
- 편지 없음(404): "편지를 찾을 수 없습니다. 링크를 다시 확인해 주세요."
- 네트워크 오류: 재시도 버튼.

**로딩:** 편지 파일을 가져오는 동안 스켈레톤 표시.

### 7.5 지난 편지 보관함 (`#/archive`) — 후원자용

선교편지는 매달 쌓인다. 후원자가 이번 달 편지만 보고 끝나지 않도록, 지난 소식을 이어서 읽을 수 있어야 한다.

- **최신 편지가 맨 위**에 오고 아래로 갈수록 지난 편지다. 맨 위 항목에는 "최신" 표시를 단다.
- 각 항목에 **기간과 발행일**을 보여준다. 이 둘은 `index.json` 에 있는 값이라 비밀번호 없이도 보인다.
- **제목은 잠겨 있다.** 비밀번호를 한 번이라도 통과한 기기에서만 제목이 풀려 보인다.
  아직 열지 않은 방문자에게는 "🔒 비밀번호를 입력하면 제목이 보입니다"로 표시한다.
- 항목을 누르면 그 편지의 보기 화면으로 간다.

**편지 사이 이동** — 편지 보기 화면 아래에 `← 지난 편지 / 편지 N통 모두 보기 / 다음 편지 →` 를 둔다.
한 통을 읽고 나서 자연스럽게 이전 소식으로 넘어갈 수 있게 한다.

**비밀번호 한 번으로 여러 편지** — 마지막으로 통과한 비밀번호를 기기에 기억해 두고,
다른 편지를 열 때 먼저 시도한다. 매달 같은 비밀번호를 쓰면 후원자는 한 번만 입력하면 된다.
편지별로 비밀번호가 다르면 그 편지에서만 다시 묻는다.

**주소만 알고 들어온 경우** — `https://<계정>.github.io/<저장소>/` 로 접속하면 최신 편지로 바로 이동한다.
후원자가 매달 링크를 새로 받지 않아도 주소 하나를 즐겨찾기 해 두면 된다.

### 7.4 설정 (`#/settings`)

- 선교사 이름
- GitHub 저장소: 소유자 / 저장소명 / 브랜치(기본 `main`)
- GitHub 토큰 (입력 시 마스킹, "연결 테스트" 버튼, 발급 방법 안내 링크)
- 기본 비밀번호
- 위험 안내: 공용 컴퓨터 경고 · 토큰 폐기 방법
- 모든 값은 이 브라우저에만 저장됨을 명시

---

## 8. 이번에 만들지 않는 것

- 후원자 로그인 — 없음
- 댓글 — 없음
- 이메일 자동 발송 — 없음 (§5 P2 참조: 서버 필요)
- 여러 기기 간 자동 동기화 — 설정은 브라우저별로 따로 입력해야 한다
- 편지 통계·조회수 — 없음
- **완전한 익명성** — 저장소가 공개면 발행 날짜와 편지 개수는 드러난다

---

## 9. 위험 요소와 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 선교사가 GitHub 토큰 발급을 어려워함 | 앱 사용 불가 | 설정 마법사에 화면별 안내 제공. P1-14로 토큰 없는 대체 경로 확보 |
| 토큰 유출 | 저장소 조작 가능 | 저장소 1개 · Contents 권한만. 공용 PC 경고. 폐기 절차 안내 |
| 약한 비밀번호 | 암호화 무력화 | 8자 이상 강제, 약한 비밀번호 경고 |
| 드라이브 사진이 후원자에게 안 보임 | 편지가 반쪽이 됨 | 붙여넣는 즉시 검증 + 발행 전 재확인 (§6.2) |
| 구글의 핫링크 제한 | 사진 로딩 실패 | 2단계 URL 폴백, 장수 제한 권장, 실패 시 대체 안내 문구 표시 |
| 브라우저 데이터 삭제로 설정 소실 | 재설정 필요 | 발행된 편지는 저장소에 있으므로 안전. 설정만 다시 입력하면 복구됨 |
| 비밀번호 분실 | **편지 영구 복호화 불가** | 발행 시 "비밀번호를 잃어버리면 편지를 열 수 없습니다"를 명확히 경고 |

---

## 10. 구현 순서 권장

1. 설정 화면 + GitHub 연결 테스트 (P0-1) — 나머지 전부의 전제
2. 암호화/복호화 유틸 (P0-4) — 단독 테스트 가능
3. 편지 보기 화면 (P0-8) — 손으로 만든 샘플 JSON으로 검증
4. 작성 + 발행 (P0-2, 5) — 여기서 처음으로 전체 흐름이 연결됨
5. 드라이브 사진 (P0-3)
6. 목록 + 수정 (P0-6, 7)
7. 인쇄 CSS (P0-9) 및 반응형 마감 (P0-10)

각 단계마다 실제 GitHub 저장소와 실제 드라이브 링크로 검증한다. 특히 **사진은 로그아웃 상태(시크릿 창)에서 반드시 확인**한다.

---

*담당자 정보: [담당자] [연락처]*
*본 문서에서 확정되지 않은 항목은 "미정"으로 표기하며, 실제 개인정보·연락처는 빈칸([ ])으로 남겨두었습니다.*
